# 🎨 AI-Email 详细设计文档

## 目录

1. [问题背景](#问题背景)
2. [系统目标](#系统目标)
3. [架构设计](#架构设计)
4. [安全模型](#安全模型)
5. [数据流程](#数据流程)
6. [API 设计](#api-设计)
7. [实现细节](#实现细节)
8. [部署方案](#部署方案)

---

## 问题背景

### 现有方案的痛点

| 方案 | 问题 |
|------|------|
| Gmail API | OAuth 流程复杂，需要用户交互授权 |
| IMAP/SMTP | 需要存储密码或应用密码，不够安全 |
| 邮件推送服务 | 需要复杂的基础设施 |
| Webhook | 需要公网访问，配置复杂 |

### AI 的需求

AI 处理邮件需要：
- ✅ 自主读取邮件（无需人类每次授权）
- ✅ 理解邮件内容
- ✅ 执行邮件中的指令
- ✅ 安全处理敏感信息
- ✅ 通过即时通讯通知

---

## 系统目标

### 核心目标

1. **零信任安全**：服务器无法查看明文内容
2. **极简配置**：人类只需 1 步配置
3. **AI 自主**：AI 可以独立工作，无需人类干预
4. **现代加密**：使用最新加密标准

### 非目标

- ❌ 替代传统邮箱（保留原有邮箱）
- ❌ 支持多人协作（单用户设计）
- ❌ 实时推送（轮询模式足够）

---

## 架构设计

### 系统组件

```
┌─────────────────────────────────────────────────────────┐
│                      用户层                              │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐          │
│  │ 人类邮箱   │  │ AI-Email  │  │  即时通讯  │          │
│  │ (转发邮件) │→ │ (加密存储) │→ │ (通知结果) │          │
│  └───────────┘  └───────────┘  └───────────┘          │
└─────────────────────────────────────────────────────────┘
                           │
                           ↓
┌─────────────────────────────────────────────────────────┐
│                      加密层                              │
│  ┌─────────────────────────────────────────────────┐   │
│  │         X25519 + ChaCha20-Poly1305              │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                           │
                           ↓
┌─────────────────────────────────────────────────────────┐
│                      AI 层                              │
│  ┌─────────────────────────────────────────────────┐   │
│  │  AI 助手（OpenClaw 或任何 AI Agent）            │   │
│  │  - 私钥解密                                      │   │
│  │  - 理解邮件内容                                  │   │
│  │  - 执行指令                                      │   │
│  │  - 发送回复                                      │   │
─────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 技术│  └────────────────────────────────选型

| 组件 | 技术选择 | 理由 |
|------|----------|------|
| 加密 | noble-cryptography | 现代、审计友好、无依赖 |
| 邮件接收 | imap-simple | 简单、稳定 |
| 邮件发送 | nodemailer | 功能完整 |
| 服务器 | Node.js 18+ | 加密性能好 |
| 部署 | Vercel/Cloudflare | 免费、易用 |

---

## 安全模型

### 密钥体系

#### X25519 密钥对

```
AI 生成：
├── 公钥 (32 bytes, base64 编码)
│   └── 可公开分享，用于加密邮件
│
└── 私钥 (32 bytes, base64 编码)
    └── 本地安全存储，永不上传
```

#### 加密流程

```
1. 邮件到达 AI-Email 服务器
2. 使用 AI 公钥加密：
   - 生成随机 Ephemeral Key (32 bytes)
   - 使用 X25519 计算共享密钥
   - 使用 ChaCha20-Poly1305 加密内容
3. 存储：(盐) + (公钥) + (密文)
4. AI 下载后使用私钥解密
```

### 安全标准

| 项目 | 标准 | 强度 |
|------|------|------|
| 密钥交换 | X25519 | 128-bit |
| 对称加密 | ChaCha20-Poly1305 | 256-bit |
| 哈希 | BLAKE3 | 256-bit |
| 随机数 | CSPRNG | 密码学安全 |

---

## 数据流程

### 邮件转发流程

```
┌──────────┐     1. 新邮件      ┌─────────────────┐
│ 人类邮箱  │ ─────────────────→ │  IMAP 轮询       │
│          │                     │  (每分钟)        │
└──────────┘                     └────────┬────────┘
                                          │
                                          ↓
                                   ┌─────────────────┐
                                   │  验证公钥存在     │
                                   └────────┬────────┘
                                          │
                                          ↓
                                   ┌─────────────────┐
                                   │  加密邮件        │
                                   │  - 生成盐        │
                                   │  - X25519 密钥   │
                                   │  - ChaCha20 加密 │
                                   └────────┬────────┘
                                          │
                                          ↓
                                   ┌─────────────────┐
                                   │  存储加密邮件     │
                                   │  SQLite/文件     │
                                   └────────┬────────┘
```

### AI 处理流程

```
┌─────────────────┐
│  获取加密邮件    │  ← 下载所有未处理邮件
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  加载私钥        │  ← 从本地存储解密私钥
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  解密邮件        │  ← X25519 + ChaCha20-Poly1305
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  AI 处理        │  ← 理解内容、提取指令
└────────┬────────┘
         │
         ├──────────────────────────────────────┐
         ↓                                      ↓
┌─────────────────┐                 ┌─────────────────┐
│  发送回复        │                 │  执行指令        │
│  (nodemailer)   │                 │  (根据内容执行)  │
└────────┬────────┘                 └────────┬────────┘
         │                                      │
         └──────────────────┬───────────────────┘
                            ↓
                   ┌─────────────────┐
                   │  即时通讯通知    │
                   │  (微信/Telegram) │
                   └─────────────────┘
```

---

## API 设计

### REST API

#### 邮箱操作

```
GET  /api/emails          # 获取邮件列表
GET  /api/emails/:id      # 获取单封邮件
POST /api/emails          # 手动触发邮件检查
DELETE /api/emails/:id    # 删除邮件
```

#### 密钥操作

```
GET  /api/key/public      # 获取公钥
POST /api/key/verify      # 验证公钥是否有效
```

#### AI 操作

```
POST /api/ai/process/:id  # 处理单封邮件
POST /api/ai/reply        # 发送回复
```

### Webhook

```
POST /webhook/email       # 邮箱轮询触发
POST /webhook/process     # AI 处理完成
```

### 认证

```
Header: Authorization: Bearer <api-key>
```

---

## 实现细节

### 核心代码结构

```
src/
├── crypto/
│   ├── KeyPair.js        # 密钥对生成
│   ├── Encrypt.js        # 加密实现
│   └── Decrypt.js        # 解密实现
├── email/
│   ├── Imap.js           # IMAP 客户端
│   ├── Smtp.js           # SMTP 客户端
│   └── Parser.js         # 邮件解析
├── storage/
│   ├── FileStore.js      # 文件存储
│   └── Database.js       # SQLite 存储
├── server/
│   ├── Express.js        # HTTP 服务器
│   └── WebSocket.js      # 实时通知
└── notify/
    ├── Telegram.js       # Telegram 通知
    └── WeChat.js         # 微信通知
```

### 关键代码示例

#### 密钥生成

```javascript
import { x25519 } from '@noble/curves/ed25519';
import { convert } from '@noble/curves/abstract/utils';

class KeyPair {
  static generate() {
    const privateKey = x25519.utils.randomPrivateKey();
    const publicKey = x25519.getPublicKey(privateKey);
    
    return {
      publicKey: convert.toBase64(publicKey),
      privateKey: convert.toBase64(privateKey)
    };
  }
  
  static savePrivateKey(privateKey, password) {
    // 使用密码加密私钥后存储
  }
}
```

#### 加密邮件

```javascript
import { x25519 } from '@noble/curves/x25519';
import { ChaPoly } from '@noble/ciphers/chacha';

class Encryptor {
  static encrypt(publicKeyBase64, plaintext) {
    const publicKey = convert.fromBase64(publicKeyBase64);
    const ephemeralPrivateKey = x25519.utils.randomPrivateKey();
    const ephemeralPublicKey = x25519.getPublicKey(ephemeralPrivateKey);
    
    // 计算共享密钥
    const sharedKey = x25519.getSharedSecret(ephemeralPrivateKey, publicKey);
    
    // 加密内容
    const cipher = new ChaPoly(sharedKey);
    const ciphertext = cipher.encrypt(new TextEncoder().encode(plaintext));
    
    return {
      salt: convert.toBase64(ephemeralPublicKey),
      ciphertext: convert.toBase64(ciphertext)
    };
  }
}
```

---

## 部署方案

### 方案对比

| 方案 | 成本 | 难度 | 扩展性 |
|------|------|------|--------|
| Vercel | 免费 | ⭐ | ⭐⭐ |
| Cloudflare Workers | 免费 | ⭐⭐ | ⭐⭐⭐ |
| VPS (DigitalOcean) | $4/月 | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 本地运行 | 免费 | ⭐⭐⭐ | ⭐ |

### 推荐方案

**个人使用**：Vercel + 本地 AI
**团队使用**：VPS + Docker

### Docker 部署

```dockerfile
FROM node:18-alpine

WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY src ./src
COPY .env.example .env

EXPOSE 3000
CMD ["node", "src/server.js"]
```

```bash
# 构建并运行
docker build -t ai-email .
docker run -p 3000:3000 --env-file .env ai-email
```

---

## 监控和维护

### 健康检查

```bash
# 检查服务器状态
curl https://your-domain.com/health

# 检查邮件同步
curl https://your-domain.com/api/emails/count
```

### 日志

```bash
# 查看日志
tail -f logs/combined.log

# 查看错误
tail -f logs/error.log
```

### 备份

```bash
# 备份加密邮件
cp -r data emails-backup-$(date +%Y%m%d)

# 备份密钥（重要！）
cp keys/private-key.encrypted keys-backup/
```

---

## 未来规划

### v1.0

- [x] 核心加密实现
- [x] IMAP 邮件接收
- [x] SMTP 邮件发送
- [x] 基础 API

### v2.0

- [ ] 多邮箱支持
- [ ] 邮件模板
- [ ] AI 指令解析
- [ ] 微信通知

### v3.0

- [ ] 插件系统
- [ ] Web UI
- [ ] 移动端 App
- [ ] 企业版功能

---

## 参考资料

- [X25519: Elliptic Curve Diffie-Hellman](https://ietf.org/rfc/rfc7748.html)
- [ChaCha20-Poly1305](https://ietf.org/rfc/rfc7539.html)
- [noble-cryptography](https://github.com/paulmillr/noble-cryptography)
- [OpenClaw 文档](https://docs.openclaw.ai/)

---

*本文档由 AI-Email 团队维护*
